<?xml version="1.0" encoding="utf-8"?>
<com.wecyberstage.wecyberstage.view.composeX.StageLineCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    card_view:cardCornerRadius="4dp">
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:id="@+id/composeXCardLine_line"
        android:orientation="horizontal">
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:src="@drawable/drag_vertical"
            android:id="@+id/composeXCardLine_dragHandle"
            />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/stageLine_dialogue"
            android:text="@string/app_name"
            android:background="@android:color/darker_gray"
            android:elevation="4dp"
            android:textSize="24sp" />
    </LinearLayout>
    <ImageView
        android:id="@+id/stageLine_mask"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/composeXCardLine_line"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:scaleType="centerInside"
        android:contentDescription="@string/app_name"
        android:src="@android:drawable/sym_def_app_icon"
        android:background="@drawable/shape_circle"
        android:elevation="4dp" />
</com.wecyberstage.wecyberstage.view.composeX.StageLineCardView>
